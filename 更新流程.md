# Android 漢化 APK 更新流程

## 一句話

> **你只需要翻譯 `Dot-abyess-Lienchu-version\dotabyss-translation-client-version-s88037zz\` 裡的文本。**
> 其他所有東西不是產物就是輸入，不要手改。

翻完之後重新 build APK，翻譯就會被**內嵌進 APK**。
Android 版**不經過 CDN**，所以 `update_manifest.py`、`verify_cdn.py` 這些
只跟 PC 版有關的步驟，做 APK 時可以完全跳過。

---

## 檔案角色（哪些能改、哪些不能）

| 路徑 | 角色 | 你要動它嗎 |
|---|---|---|
| `Dot-abyess-Lienchu-version\...s88037zz\` | **唯一的翻譯來源** | ✅ **只改這裡** |
| ├─ `static/`、`ui_texts/`、`names/`、`add-on/`、`other/` | UI／系統文字 | ✅ 直接改 |
| ├─ `novels/`、`novels_untranslated_only/` | 劇情**來源** | ✅ 直接改 |
| └─ `novels_*_all/` | 劇情**分包（產物）** | ❌ 由工具重建 |
| `hook\DotAbyssHook-frida\src\embedded-translations.ts` | 產物（17 MB） | ❌ 每次 build 自動重生 |
| `hook\DotAbyssHook-frida\res\ttcuyuanj` | 中文字型 bundle | ❌ 除非遊戲換 Unity 版本 |
| `hook\DotAbyssHook-frida\apk\*-official.apk` | 官方原始 APK | ❌ 由 build.py 下載 |

> 翻譯來源寫在 `tools/generate-embedded-translations.mjs`：
> `DOTABYSS_TRANSLATIONS` 環境變數 → 同層的 `..\..\Dot-abyess-Lienchu-version\...s88037zz\`。
> **刻意沒有備援副本**——曾經有兩份，但退到副本會安靜地內嵌舊翻譯：
> 你改了真實 repo、build、出貨舊資料，全程沒有任何錯誤訊息。
> 現在找不到就直接 throw，寧可爆掉也不要猜。
> 每次 build 都會印出 `translation source: <路徑>`，**指錯一眼就看得到**。

---

## 情境 A：只改了翻譯（官方版本沒變）

最常見。約 **2 分鐘**。

```bash
# 1. 劇情有改才要這步：重建分包（遊戲讀分包，不讀逐檔）
cd Dot-abyess-Lienchu-version\dotabyss-translation-client-version-s88037zz
python tools\build_novels_all.py
```

```bash
# 2. 重新內嵌 + 重打包 + 重簽
cd hook\DotAbyssHook-frida
python build.py --reinject
```

```bash
# 3. 安裝（同一把 key，存檔不會掉）
%LOCALAPPDATA%\Android\Sdk\platform-tools\adb.exe install -r dist\DotAbyssX-R18-zh-Hant.apk
```

⚠️ `--reinject` **只換 gadget 的三個檔案**（腳本／gadget／設定）再重簽。
它**不會**重新處理 assets、也不會做 smali patch。所以只有在
「官方 APK 沒換版」時才能用。

**但你不需要自己記得這件事**——`--reinject` 會先向 DMM API 查最新版，
和手上這顆的 `versionName` 比對：

```
Game version 1.7.0 is current.          ← 一致，繼續
```

```
refusing --reinject: it would rebuild on the OLD game version.
  patched APK base : 1.7.0
  latest official  : 1.8.0
Run a full build instead:  python build.py
```

查不到 API（沒網路等）時只會印警告不會擋，所以離線也還能用。

> 翻譯是包在 gadget 腳本裡的，而 `npm run build` 在 `--reinject` 分支**之前**執行，
> 所以 `--reinject` 確實會重新產生並內嵌最新翻譯。這點已實測確認。

---

## 情境 B：官方改版了（要重新下載 APK）

約 **20 分鐘**（apktool 解包／回包很慢）。

```bash
# 1. 劇情有改才要這步
cd Dot-abyess-Lienchu-version\dotabyss-translation-client-version-s88037zz
python tools\build_novels_all.py
```

```bash
# 2. 完整重建：不給 --input 就會自動向 DMM API 查最新版並下載
cd hook\DotAbyssHook-frida
python build.py
```

已經有官方 APK 檔的話：

```bash
python build.py --input apk\DotAbyssX-<版本>-official.apk
```

```bash
# 3. 安裝
%LOCALAPPDATA%\Android\Sdk\platform-tools\adb.exe install -r dist\DotAbyssX-R18-zh-Hant.apk
```

完整建置會依序做：
讀真實 repo 的翻譯 → 產生 `embedded-translations.ts` → frida-compile →
`apktool d` → patch `UnityPlayerActivity`（加 `System.loadLibrary("gadget")`）→
刪除 armeabi-v7a → 複製 `res/ttcuyuanj` 到 `assets/bin/Data/Managed/` →
`apktool b` → 注入 gadget → `zipalign` → `apksigner`。

> `assets/bin/Data/Managed/` 底下的東西，Unity 會在**第一次啟動時**解到
> `persistentDataPath/il2cpp/`，字型就是從那裡載入的。
> 所以字型**必須靠完整重建才會進 APK**；`adb push` 推的那份重裝就沒了。

---

## 驗證清單（每次建置後）

```
□ build log 印的 translation source 指向你剛改過的那份 Dot-abyess-Lienchu-version
□ build log 有 "Detected base APK: package=jp.co.fanzagames.dotabyss_x_a"
□ build log 有 "Restored N res/ entries..." 或 "Resource check: nothing lost"
□ build log 有 "Verified using v2 scheme ... true"
□ 裝上去、照儀式開一次遊戲
□ adb logcat -s DotAbyssHook:* 檢查下面四項
```

> **`Restored N res/ entries` 這行一定要有**（或是「nothing lost」）。
> apktool 在 Windows 上會把 `res/S0.png` 和 `res/s0.png` 這種只差大小寫的
> 混淆資源壓成同一個檔，回包時少掉 50 個 —— 而 `resources.arsc` 還指著它們。
> 2026-08-12 就是這樣讓朋友的 Pixel 9a 一直閃退（我的 OPPO 完全正常，
> 因為兩台的螢幕密度桶不同，解析到的檔案不一樣）。詳見 `NOTES.md` 第十節。
> **這種傷害「在我手機上正常」完全證明不了**，只能靠 build log 這行。

> **已於 2026-08-11 完整驗證通過**（官方 1.7.0）：UI／角色名／靜態標籤／劇情
> 全部翻譯正常，方框字已解決，`GAME START` 按鈕正常顯示。
> 字型由 APK 內建的 `il2cpp/ttcuyuanj` 載入，非手動推送。

logcat 該出現的：

```
hooked TMPro.TMP_Text.set_text
hooked TMPro.TextMeshProUGUI.OnEnable
hooked Absf.Novel.NovelArgument.SetString      ← 劇情
FONT OK: "TTCuYuanJ SDF" from ttcuyuanj added to TMP fallbacks   ← 字型
```

畫面上該確認的：

| 看哪裡 | 確認什麼 |
|---|---|
| 主畫面底部 | `首頁 / 隊伍 / 娼館 / 商店 / 轉蛋 / 任務 / 探索隊` 是中文 |
| 隊伍分頁的對話框 | `讓我見識一下妳的本事吧！` —— **`妳`、`吧` 不是方框** |
| 隨便一段劇情 | 對話是中文 |
| 標題畫面 | `GAME START` 按鈕**有字**（字型版本不對時會整個空白） |

---

## 啟動儀式（每次開遊戲都要）

1. Planet VPN 連 `Japan - Osaka`，等畫面顯示 `You are protected`
   （或用 `adb shell "ip -o addr show | grep -c tun"` 回傳 2 來確認）
2. 開遊戲
3. 紫色 `LOADING` 出現後約 **8～11 秒**、還沒到 `GAME START` 時，
   從通知列按 `Disconnect`
4. 回到遊戲

---

## 新增翻譯分類時的陷阱

`tools/generate-embedded-translations.mjs` 是用正規表示式掃
`src/config.ts` 裡的 `"*/zh_Hant.json"` 路徑來決定要包哪些檔。

**在資料 repo 新增一個分類資料夾（例如 `other/新東西/`），
不同步加進 `src/config.ts` 的 `translationFiles`，它就不會被包進 APK，
而且沒有任何錯誤訊息。**

---

## 官方換 Unity 版本時

字型 bundle 必須跟遊戲同一個 Unity 版本，否則文字會**整個消失**（不是方框）。
版本字串就寫在 bundle 檔頭前 40 bytes：

```bash
python -c "print(open(r'res\ttcuyuanj','rb').read(40))"
```

目前：`ttcuyuanj` = `6000.3.8f1`，與遊戲相符。
（`res/notosanscjktc` 是 `2021.3.25f1`，來自別的專案，**不能用**，
用了會讓 `GAME START` 之類的按鈕變成空白。）

不相符的話要用該版本的 Unity 重新編一包字型 AssetBundle，
資產名要是 `<bundle 名> SDF`。

---

## 疑難排解

| 症狀 | 先查 |
|---|---|
| 整包翻譯都沒生效 | logcat 有沒有 `injector started`、`hooked ...` |
| 只有劇情沒翻 | `hooked Absf.Novel.NovelArgument.SetString` 在不在 |
| 方框字回來了 | `FONT OK` 在不在；不在的話看 `FONT FAILED at <步驟>` |
| 按鈕文字空白 | 字型 bundle 的 Unity 版本跟遊戲不合 |
| 改了翻譯卻沒變 | build log 的 `translation source` 指到哪 |
| 新分類沒生效 | `src/config.ts` 的 `translationFiles` 有沒有加 |

更深入的技術細節、以及開發時踩過的坑，見 `NOTES.md`。
